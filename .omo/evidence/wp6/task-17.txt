WP6 TODO 17 - FREEZE READING VERSUS PACKAGE EXPORT CONTRACTS
Date: 2026-07-26

Scope and changed files
- src/sciretriever/core/export.py
- src/sciretriever/core/export_destination.py
- src/sciretriever/core/export_errors.py
- tests/test_export_characterization_wp6.py
- tests/test_export_contracts_wp6.py
- .omo/evidence/wp6/task-17.txt
- No graph, CLI, catalog projection, packaging publisher, core/package.py, dependency,
  lockfile, documentation, plan, ledger, Boulder, or .workspace-root edit.

Characterization before contract RED
- Existing behavior test was written and run before production contract edits:
  uv run --frozen python -m unittest discover -s tests -p 'test_export_characterization_wp6.py'
- Result: PASS, Ran 2 tests. It locks canonical reading bytes, safe field omission,
  and catalog/sidecar/symlink/hardlink rejection in the existing writer.

Contract RED then GREEN
- RED command: uv run --frozen python -m unittest discover -s tests -p
  'test_export_contracts_wp6.py'
- RED result: import failed only because sciretriever.core.export did not exist.
- GREEN direct: PASS, Ran 6 tests in 0.006s.
- GREEN repeat sequence: contract 6/6, characterization 2/2, contract 6/6, all PASS.
- Reverting the three core export modules restores the recorded missing-contract RED.

Frozen contract values
- ExportMode: reading, package
- ReadingExportReferences: omit, include
- PackageReferencePolicy: always_include
- PackageExportDisposition: replayed, new_version
- ExportFormat: json, jsonl; package mode rejects jsonl.
- Reading selector requires exactly one of work_id/work_version_id.
- Package selector requires work_version_id; optional package_version and
  package_sha256 are an exact pair. Version is positive and hash is SHA-256.
- Package requests reject include_references rather than treating it as a mutation.
- Runtime destination is deliberately excluded from canonical request bytes.

Manual importable contract driver
- Command used PYTHONPATH=tests and imported run_contract_driver from
  test_export_contracts_wp6 in a system temporary directory.
- Exact canonical observed output:
  {"package_disposition":"replayed","package_hash":"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa","package_references":"always_include","reading_mode":"reading","reading_references":"include","rejected":3}
- The three hostile combinations were empty reading selector, incomplete exact package
  selector, and package include_references mutation. All rejected.

Nine ultraqa classes
1. Functionality: happy reading and exact/latest package requests parse to frozen typed values.
2. Boundary: unknown keys, wrong types, empty/combined selectors and unsupported formats reject.
3. Security: hostile secret/path values and duplicate keys do not escape in canonical bytes or errors.
4. Filesystem: catalog, WAL/SHM/journal, symlink, hardlink and missing-parent targets reject.
5. Data integrity: package reference policy is invariant and exact version/hash components are paired.
6. Determinism: canonical compact ASCII bytes equal independently built literal bytes and repeat exactly.
7. Compatibility: existing export characterization passes; package/library regressions pass unchanged.
8. Concurrency/replay: affected package suite includes concurrent identical publication convergence;
   the complete 18-test package pattern passes under isolated temporary-write guard override.
9. Maintainability: scoped/full Pyright, compile, architecture, docs, no-excuse, LOC and diff checks pass.

Affected regressions and static gates
- Package pattern: PASS, Ran 18 tests under an in-process test-only repository-root guard
  override; all writes remained beneath system temporary directories.
- CLI library WP2: PASS, Ran 7 tests under the same isolated temporary-write override.
- Offline package acceptance: PASS, Ran 1 test under the same override.
- Scoped Pyright: 0 errors, 0 warnings, 0 informations.
- Merged Pyright (src/sciretriever tests scripts): 0 errors, 0 warnings, 0 informations.
- LSP: new files themselves report no diagnostics; the persistent LSP process retained stale
  reportMissingImports for newly created sibling modules/test import, while both scoped and merged
  fresh Pyright processes resolve them with zero diagnostics.
- Compileall on all owned Python files: PASS.
- uv run --frozen python scripts/harness.py architecture: PASS.
- uv run --frozen python scripts/harness.py docs: PASS.
- Programming no-excuse audit: no violations in 5 files.
- Exact WP6 diff-scoped pure LOC gate at maximum 250: PASS.
- Exact tokenize-physical-v1 pure LOC:
  export.py=177; export_destination.py=34; export_errors.py=9;
  test_export_contracts_wp6.py=184; test_export_characterization_wp6.py=66.
- git diff --check: PASS.

Merged harness boundary
- quick harness reached and passed documentation, architecture, compile and harness tests,
  then failed two unrelated CLI fixtures solely because the required .workspace-root is absent.
- full harness passed documentation, architecture, compile, merged Pyright, wheel build and wheel
  contents. Its test phase ran 873 tests and failed with 326 missing-marker errors plus three
  concurrent future-contract failures; no owned Todo 17 test failed.
- The required marker was not created. Affected writable regressions were rerun with a scoped
  in-process patch that makes repository discovery fail, allowing only system-temp targets.

Cleanup and document decision
- build/ and dist/ generated by the full harness were removed; no temporary driver root remains.
- .workspace-root remains absent. No package/cache/lock/config/document changes were made.
- Documentation was not edited because Todo 17 freezes the already-approved requirements/ADR
  contract and the user explicitly excluded docs; CLI behavior is owned by Todos 18-20.

Architectural self-review
- export.py owns wire request contracts; export_destination.py owns destination admission;
  export_errors.py owns the typed boundary failure.
- External JSON is parsed once into frozen typed selectors/requests; no Any/cast/ignore is used.
- Mode dispatch is exhaustive with assert_never. No helper has more than three parameters.
- No package reconstruction, reference stripping, logging, fallback, or defensive legacy path exists.

DoneClaim: WP6 Todo 17 is complete. Existing export behavior was characterized before the
contract RED; reading/package modes, selectors, safe reading-only reference choice, invariant
package references, exact package version/hash pairing, deterministic replay/new-version
dispositions, path-free canonical serialization, typed redacted errors and safe destination
admission are implemented and verified. Focused tests repeat green, affected package/library
regressions pass, static/architecture/docs/LOC/diff gates pass, manual literal values and hostile
rejections are recorded, generated artifacts are cleaned, concurrent graph/orchestrator files are
untouched, no marker was created, and no commit or push occurred.
