# Agent 开发与质量 Harness

> 本文件定义稳定的工程工作流、代码质量基线、机械门禁、Git 纪律和通用完成标准。SciRetriever 的产品边界、架构约束、项目命令入口和真相源由根目录 [AGENTS.md](AGENTS.md) 定义。修改仓库前应先读 `AGENTS.md`，再读本文件。

## 1. 职责、权威与规则强度

本文件回答“如何可靠地完成和验证修改”，不定义产品需求、业务数据含义或目标架构。项目级架构规则必须能够追溯到 requirements、Accepted ADR、design 或 technical；`AGENTS.md` 可以汇总和收紧这些规则，但不能静默降低安全边界。

| 强度 | 含义 |
| --- | --- |
| POLICY | 由实现者自检和语义审查保证，不能仅凭命令判定 |
| LOCAL GATE | 可由本地命令机械检查，但不假定每个环境都安装了 hook |
| CI GATE | 由 CI 执行并阻断失败变更 |
| HUMAN GATE | 需要授权人员对边界、风险或不可逆影响作出明确决定 |

机械门禁只实现项目 owner 明确批准且工具能够稳定、无上下文歧义地判断的规则：

- 不把对话总结、执行计划、审查意见、通用经验或自行推导的约束新增为 CLI 检查、AST 解释器、固定阈值或阻断条件。
- 未获 owner 批准的约束保留为 POLICY、测试意图或人工审查事项。
- `scripts/harness.py` 定义可执行步骤，`pyproject.toml` 定义原生工具配置，`.github/workflows/ci.yml` 定义 CI 运行方式；三者变化时必须同步本文件和 `AGENTS.md` 的命令入口。
- 文档中的命令或路径与仓库不符时，先核实真实入口，不虚构工具，也不把未执行的能力写成门禁。

## 2. 标准工作流

所有非平凡任务遵循以下顺序：

1. **确认意图**：提取可验证目标、约束、授权范围和完成条件。
2. **读取上下文**：检查工作树、实现、调用方、直接测试、配置和项目真相源。
3. **确定范围**：明确责任边界、风险、兼容性、数据影响和文档影响。
4. **实施改动**：沿用已接受模式，完成最小但完整的根因修复。
5. **分层验证**：先运行最小复现、相关测试和静态检查，再按风险扩大范围。
6. **语义复核**：重新对照原始目标，检查最终 diff、诊断、文档同步和残余风险。
7. **提交或交付**：仅在用户授权时执行 Git 写操作；说明结果、验证证据和未完成事项。

用户只要求解释、诊断、审查或方案时，不越权实施修改；用户明确要求修改或构建时，不得无故停在分析阶段。

## 3. 工作树、文件与外部系统安全

### 3.1 现有改动

- **POLICY**：默认工作树可能不干净；未确认来源的内容视为用户改动。
- **POLICY**：不得回滚、覆盖、格式化掉或顺手提交任务无关改动。
- **POLICY**：任务文件已有改动时，先理解差异并在其基础上工作。
- **POLICY**：未经明确授权，不使用会丢失工作树、索引或历史内容的破坏性命令。

### 3.2 临时文件与生成物

- 临时验证文件只放在 `AGENTS.md` 指定的位置，优先使用系统临时目录。
- 不在仓库根目录散落一次性脚本、日志、下载文件、数据库或数据集。
- 生成物通过项目规定的命令产生，不手工编辑生成文件。
- 不创建平行虚拟环境、依赖缓存或工具目录，除非项目画像要求或用户明确授权。
- 构建和测试生成的文件在交付前检查；不得作为无关内容暂存或提交。

### 3.3 凭据与外部系统

- 不读取、打印、修改或提交 `.env`、密钥、token、生产凭据和个人配置中的敏感值。
- 不硬编码服务器地址、用户名、密钥或环境特定绝对路径。
- 测试和 Harness 不连接真实供应商、生产数据库或用户语料。
- 不访问生产数据、不执行生产变更、不向外部系统写入，除非用户明确授权且项目权限允许。

## 4. 实施质量原则

### 4.1 先理解再修改

- 先查看同模块实现、调用方、直接测试和适用合同，再决定抽象与命名。
- 优先使用仓库已有框架、模式和公共 helper，不建立平行体系。
- 不推测未读取代码的行为；用搜索、类型、测试或运行结果验证。
- 修改共享契约前检查生产者、消费者、序列化边界、持久化影响和兼容策略。

### 4.2 修复根因

- 不增加只为当前测试成立的分支，不通过异常吞噬或多层动态探测掩盖内部接口不完整。
- 兼容逻辑集中在明确的 adapter 或 compatibility boundary，并有删除或版本边界。
- 只有确实降低重复、耦合或认知复杂度时才增加抽象。
- 长函数、深嵌套、boolean flag soup、magic string、裸字典和散落 fallback 是设计信号，应先检查契约和责任归属。

### 4.3 边界、类型与错误

- HTTP、CLI、配置、数据库动态字段、第三方响应、插件和环境变量都属于外部输入，必须在边界解析、验证和规范化。
- 内部逻辑只消费明确类型、schema、协议或领域对象；封闭状态集合使用 `Enum` 或等价类型。
- 不用类型抑制、忽略诊断或关闭检查器逃避设计问题。
- 错误只在能够增加上下文、完成协议转换或形成稳定用户结果的层处理，并保留异常链。
- 禁止空 `except` 和无记录吞错；顶层 catch-all 必须保留 traceback，随后转换为稳定退出结果或重新抛出。
- 资源使用 context manager 或等价的确定性清理机制。

### 4.4 复杂度与测试设计

- 函数和模块保持单一责任，控制流优先线性可读。
- 出现深层嵌套、高圈复杂度或多处同步修改时，应评估状态对象、策略、值对象或统一注册表。
- 拆分必须改善语义、依赖和测试边界，不做机械切碎。
- 注释解释原因、约束和权衡，不复述代码。
- 新行为、错误路径和回归修复使用 red-green-refactor；测试名称和分段明确表达 Given、When、Then。

项目特有的 Pydantic、六层职责、Ports 和数据所有权规则由 `AGENTS.md` 及其架构真相源定义，不在本文件重复维护。

## 5. Python 基线与机械门禁

“活动 Python 代码”由 `scripts/harness.py` 生成一份排序后的统一文件清单，范围为：

- 项目根目录现有的 `*.py`；
- `src/sciretriever/**/*.py`；
- `tests/**/*.py`；
- `scripts/**/*.py`。

Ruff、Ruff format check、`compileall` 和 Pyright 接收同一份清单。`archive/`、`.venv/`、`build/`、`dist/`、缓存、生成物和 vendored code 不进入这组检查；测试发现和 wheel 内容门禁使用各自明确的确定性范围。

### 5.1 Harness 模式

| 模式 | 命令 | 实际执行内容 | 使用场景 |
| --- | --- | --- | --- |
| Quick | `uv run --frozen python scripts/harness.py quick` | Ruff lint、Ruff format check、compileall、Pyright、Harness 自测 | 开发循环与代码改动的快速机械检查 |
| Full | `uv run --frozen python scripts/harness.py full` | Quick 全部内容、完整 unittest、干净 wheel 构建与源码内容核对 | 交付前与 CI |

### 5.2 确定性门禁

- **LOCAL GATE / CI GATE**：最低运行版本是 Python 3.10，开发基线是 Python 3.12；语法、typing API 和标准库 API 必须兼容 Python 3.10。CI 在 Python 3.10 和 3.12 分别运行 Full Harness。
- **LOCAL GATE / CI GATE**：Ruff 是唯一 formatter 和 linter，配置位于 `pyproject.toml`；当前目标版本为 `py310`，行宽为 100，并启用项目选择的原生规则和 E501。
- **LOCAL GATE / CI GATE**：Pyright 使用 strict 模式和 Python 3.10 基线；错误不得降级为 warning，也不维护债务 baseline。
- **LOCAL GATE / CI GATE**：`compileall` 和标准库 `unittest` 是独立门禁。测试必须报告实际执行数量，发现零个测试时失败。
- **LOCAL GATE / CI GATE**：Full Harness 从清理后的 staging 构建 wheel，并核对 wheel 中的 Python 模块与 `src/` 一致，防止已删除模块从缓存进入发布物。
- **LOCAL GATE / CI GATE**：Ruff、Pyright 和其它原生工具的抑制必须包含具体错误码，并保持在最小语句范围；禁止文件级全局抑制。
- **LOCAL GATE / CI GATE**：Harness 直接传播原生工具的非零退出结果，不复制实现 Ruff、Pyright、Python、Pydantic 或控制流语义。

不得通过排除活动路径、降低错误级别、跳过步骤、修改测试发现范围或把失败转换为 warning 获得绿色结果。需要理解业务、职责或风险的规则属于测试和语义审查，不应新增项目自有语义解释器。

## 6. 测试、验证与语义审查

| 层级 | 内容 | 默认适用范围 |
| --- | --- | --- |
| L0 | diff、修改文件诊断、最小复现和相关测试 | 所有改动 |
| L1 | 编译、格式、lint、类型和 Harness 自测 | 所有代码改动 |
| L2 | 模块组装、序列化合同、构建或真实依赖的离线冒烟 | 跨模块、配置或契约改动 |
| L3 | 全部测试、集成、E2E 和关键行为路径 | 用户流程或运行时改动 |
| L4 | 需求、架构责任、安全边界与最终 diff 的语义审查 | 中高风险改动 |
| L5 | 授权人员审批 | 产品边界、生产、安全、公开合同和受支持数据迁移 |

验证规则：

- 开发循环先运行最小复现和直接测试，再运行 Quick Harness；完成前运行任务适用的最高层级，代码交付默认运行 Full Harness。
- 新行为、错误路径和回归场景必须有直接测试；实现与直接测试属于同一个原子变更。
- 不删除、跳过或弱化失败测试来获得绿色结果。
- Mock 只隔离真正的外部依赖，不得 mock 掉本次要证明的组装关系、事务边界或序列化合同。
- 失败时先定位根因；修复后重新运行最小复现，再扩大验证范围。

公开合同、catalog、storage、network、身份、状态、provenance、外部访问、安全、持久化或模块边界变化时，必须做专项语义审查。审查应读取原始需求、适用 ADR、责任文档、最终 diff 和验证证据，检查机械工具无法判断的责任归属、类型抑制必要性、validator 含义、异常边界、不可变性和复杂度。是否由第二位 reviewer 承担，取决于风险、可用协作方式和用户授权；低风险改动可以由实现者按同一维度自审。

## 7. 文档治理

- 同一事实只指定一个真相源，其它文档通过链接解释，不复制一份独立定义。
- requirements 说明用户问题和产品验收，architecture 说明理想设计，README 和用户指南说明当前行为，源码与测试提供实际证据；历史材料和执行计划不冒充现状。
- 以下变化必须检查文档影响：公开 API、CLI、配置、schema、序列化格式、持久化语义、默认值、模块职责、依赖方向、入口、核心术语、验证命令和权限范围。
- 代码修改按 `docs/development/documentation-map.md` 查找责任文档。只修改文档时也应检查链接、术语、事实来源和与实际命令的一致性。
- 活动提案只保留未决方向；完成、拒绝或被替代后进入归档。计划、进度和工作包证据不进入需求、设计或当前用户文档。

## 8. Git 与提交

执行 Git 写操作前，检查当前分支或 detached 状态、上游关系、`git status`、未暂存和已暂存 diff、最近提交风格及精确文件列表。

- 只暂存本任务文件，不夹带用户的无关改动。
- 一个提交只表达一个可独立理解、审查和回滚的意图。
- 实现、直接测试和必要契约文档同提交，不为拆分而留下不可运行的中间状态。
- 除非 `AGENTS.md` 明确覆盖，使用英文 Conventional Commits：`<type>(<scope>): <imperative summary>`。
- 只有用户明确要求时才 commit、amend、rebase、push、创建 PR 或发布。
- 不跳过 hook、不强推、不改写共享历史、不修改 Git 配置，除非用户明确授权且影响已知。

## 9. 权限边界

| 级别 | 操作 |
| --- | --- |
| 默认允许 | 读取代码与文档、编辑任务范围文件、运行已有检查、增加直接测试 |
| 需要明确授权或人工判断 | 安装或升级依赖、修改 lockfile、受支持数据库迁移、删除材料文件、生产操作、公开边界变化、高资源或外部写入任务 |
| 禁止 | 读取或提交凭据、访问未授权生产数据、破坏性 Git、绕过质量门、未经授权的外部写入 |

`AGENTS.md` 和具体任务可以进一步收紧权限，不得默认放宽。

## 10. 通用完成标准

- [ ] 原始需求和用户追加约束均已处理。
- [ ] 改动遵循项目真相源、架构责任和既有模式。
- [ ] 修改文件没有新增诊断错误，相关测试通过。
- [ ] 适用的最高验证层级已经通过；代码交付的 Full Harness 已通过，或明确报告未运行原因。
- [ ] 文档同步完成，或说明无需同步的理由。
- [ ] 最终 diff 不包含无关改动、凭据、个人配置、临时文件或不应提交的生成物。
- [ ] Git 和外部操作没有超出用户授权。
- [ ] 交付说明包含结果、验证证据、未运行检查和残余风险。

Harness 通过只证明已配置的工具和测试通过，不代表业务、性能、安全或架构必然正确；无法机械检查的内容仍需领域测试、语义审查和必要的人工判断。
