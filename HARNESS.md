# Agent 开发 Harness 基线

> 本文件定义跨项目稳定的协作规则。项目命令、目录、架构和真相源由根目录 [`AGENTS.md`](AGENTS.md) 提供。修改仓库前应先读 `AGENTS.md`，再读本文件。

## 1. 规则强度

- **POLICY**：由 Agent 自检和人工审查保证。
- **LOCAL GATE**：可由本地命令检查，但不保证每个环境都已安装 hook。
- **CI GATE**：由 CI 阻断合并。
- **HUMAN GATE**：需要授权人员明确批准。

`AGENTS.md` 可以增加项目规则或提高规则强度，但不得静默降低安全边界。若文档中的命令或路径与仓库不符，先核实真实入口，不得虚构工具或把未执行的能力写成门禁。

## 2. 标准工作流

所有非平凡任务遵循以下顺序：

1. **确认意图**：提取可验证目标、约束和完成条件。
2. **读取上下文**：检查工作树、实现、调用方、测试、配置和真相源。
3. **制定范围**：明确职责边界、风险、兼容性和文档影响。
4. **实施改动**：遵循现有模式，做最小但完整的根因修复。
5. **分层验证**：先相关测试和静态检查，再按风险运行完整检查。
6. **检查结果**：复读需求，检查 diff、诊断、文档同步和残余风险。
7. **提交或交付**：仅在用户要求时提交；说明结果、验证证据和未完成事项。

除非用户只要求解释或方案，不得停在分析或建议阶段。

## 3. 工作树与文件安全

### 3.1 现有改动

- **POLICY**：默认工作树可能不干净；未确认来源的内容视为用户改动。
- **POLICY**：不得回滚、覆盖、格式化掉或顺手提交任务无关改动。
- **POLICY**：相关文件已有改动时，先理解并在其基础上工作。
- **POLICY**：禁止会丢失工作树内容的破坏性命令，除非用户明确授权并已知影响。

### 3.2 临时文件与生成物

- 临时验证文件只放在 `AGENTS.md` 指定的位置。
- 不在仓库根目录散落一次性脚本、日志、下载文件或数据集。
- 生成物通过项目规定的命令更新，不手工编辑生成文件。
- 不创建新的虚拟环境、依赖缓存或工具目录，除非项目画像要求或用户明确授权。

### 3.3 凭据与外部系统

- 不读取、打印、修改或提交 `.env`、密钥、token、生产凭据和个人配置中的敏感值。
- 不硬编码服务器地址、用户名、密钥或环境特定绝对路径。
- 不访问生产数据、不执行生产变更、不向外部系统写入，除非用户明确授权且项目权限允许。

## 4. 代码修改原则

### 4.1 先理解再修改

- 先查看同模块实现、调用方和直接测试，再决定抽象与命名。
- 优先使用仓库已有框架、模式和公共 helper，不建立平行体系。
- 不推测未读取代码的行为；使用搜索、类型、测试或运行结果验证。
- 修改共享契约前检查实现方、消费者、序列化边界和兼容层。

### 4.2 修根因而不是堆补丁

- 长函数、深嵌套、boolean flag soup、magic string、裸字典和散落 fallback 是设计信号，应先检查契约或模型。
- 不增加只为当前测试成立的分支。
- 不用多层动态探测或异常吞噬掩盖内部接口不完整。
- 兼容逻辑集中在明确的 adapter 或 compatibility boundary。
- 只有真实降低重复或复杂度时才增加抽象。

### 4.3 边界解析与内部契约

- HTTP、CLI、JSON/YAML、数据库动态字段、第三方响应、插件和环境变量都是外部输入。
- 外部输入在边界完成解析、验证和规范化。
- 核心逻辑只消费明确类型、schema、数据类、协议或领域对象。
- 封闭状态集合使用 Enum 或等价类型；重要字符串和数值使用常量或值对象。

### 4.4 类型与错误处理

- 不用类型抑制、忽略诊断或关闭检查器逃避设计问题。
- 错误只在能够增加上下文或完成协议转换的层处理。
- 禁止空 catch/except，不得无记录吞掉异常。
- 用户可见或跨边界错误应稳定、结构化且可测试。

### 4.5 复杂度预算

- 函数和模块保持单一职责，控制流优先线性可读。
- 出现四层以上嵌套、高圈复杂度或多处同步修改时，应评估状态对象、策略、值对象或统一注册表。
- 拆分必须改善语义和测试边界，不做机械切碎。
- 注释解释原因、约束和权衡，不复述代码。

## 5. 测试与验证

| 层级 | 内容 | 默认范围 |
|---|---|---|
| L0 | diff、诊断、相关测试 | 所有改动 |
| L1 | 编译、格式、lint、类型、结构规则 | 所有代码改动 |
| L2 | 模块组装、生成契约、真实依赖冒烟 | 跨模块或契约改动 |
| L3 | 集成、E2E、关键行为路径 | 用户流程或运行时改动 |
| L4 | 需求与 diff 的语义审查 | 中高风险改动 |
| L5 | 授权人员审批 | 安全、生产、公开契约、数据迁移 |

项目画像必须把真实命令映射到这些层级。开发循环使用 `quick_check`，完成前运行适用的最高层；CI 运行项目定义的 `full_check`。wheel 门禁必须从干净 staging 构建，并核对发布包中的 Python 模块与源码一致，避免已删除模块从构建缓存进入发布物。

- 新行为、错误路径和回归场景必须有直接测试。
- 实现与直接测试属于同一个原子变更。
- 不删除、跳过或弱化失败测试来获得绿色结果。
- Mock 只隔离真正的外部依赖，不得 mock 掉本次要证明的组装关系。
- 失败时先定位根因；修复后运行最小复现，再扩大验证范围。

## 6. 文档治理

- `docs/guides/` 只解释当前软件的使用方法，不重新定义产品设计。
- `docs/development/` 保存开发入口、代码文档同步和集成检查，不描述当前用户行为。
- `docs/architecture/` 定义理想产品需求、模块边界、依赖方向、关键数据流和已接受架构决策，不记录阶段进度。
- `docs/notes/` 保存供应商和外部工具的易变事实，不替代代码、README 或架构文档。
- `docs/proposals/` 只保存 `draft` 或 `under-review` 的活动提案；提案确认方向、完成、拒绝或被替代后必须及时移入 `docs/archive/`。
- `.omo/plans/` 保存 OMO 执行计划。执行计划不进入 `docs/`，完成后也不作为项目文档归档。
- `docs/archive/` 保存已完成或被替代的提案、评估、进度和工作包证据；归档不定义当前行为，也不授权实施。
- 活动文档通过 `docs/README.md` 导航。提案收口后必须归档，不得在活动目录长期维护第二份进度或过程真相源。

以下变化必须检查文档影响：公开 API、CLI、配置、schema、序列化格式、持久化语义、默认值、模块职责、依赖方向、入口、核心术语、验证命令和权限范围。

同一事实只指定一个真相源，其他文档通过链接解释。README 和代码描述当前用户行为，architecture 描述理想软件，notes 记录外部易变事实；三者不混写。历史材料不得冒充现状。

## 7. Git 与提交

提交前必须查看当前分支、上游关系、`git status`、未暂存和已暂存 diff、最近提交风格及精确文件列表。

- 只暂存本任务文件，不夹带用户的无关改动。
- 一个提交只表达一个可独立理解、审查和回滚的意图。
- 实现与直接测试同提交；不为拆分而留下不可运行的中间状态。
- 除非 `AGENTS.md` 覆盖，使用英文 Conventional Commits：`<type>(<scope>): <imperative summary>`。
- 只有用户明确要求时才 commit、amend、rebase、push、创建 PR 或发布。
- 不跳过 hook、不强推、不改写共享历史、不修改 Git 配置，除非用户明确授权。

## 8. 权限边界

| 级别 | 操作 |
|---|---|
| 允许 | 读取代码与文档、编辑任务范围文件、运行已有检查、增加直接测试 |
| 需确认 | 安装或升级依赖、数据库迁移、删除文件、修改根治理规范、执行高资源任务 |
| 禁止 | 读取或提交凭据、访问生产数据、破坏性 Git、绕过质量门、未经授权的外部写入 |

项目画像可进一步收紧权限，不得默认放宽。

## 9. 审查原则

- 优先寻找行为回归、契约破坏、数据风险、安全问题和缺失测试。
- 先报告具体问题和文件位置，再给总体摘要。
- 小改动不堆叠重复审查；中高风险改动在完整实现后集中审查。
- 只有真实风险或不确定结论才增加专项审查。
- 审查意见应可操作，并区分 blocker、warning 和建议。

## 10. 完成标准

- [ ] 原始需求的每一项均已处理。
- [ ] 改动遵循项目架构和既有模式。
- [ ] 修改文件没有新增诊断错误。
- [ ] 相关测试通过。
- [ ] 适用的 `full_check` 通过，或明确报告未运行原因。
- [ ] 文档同步完成，或说明无需同步的理由。
- [ ] diff 中没有无关改动、凭据或临时文件。
- [ ] Git 操作符合用户授权范围。
- [ ] 交付说明包含结果、验证证据和残余风险。

Harness 通过只证明已覆盖的规则和测试通过，不代表业务、性能或安全必然正确；无法机械检查的内容仍需领域测试和人工判断。
